{
  "name": "claude-app-gateway-infra",
  "private": true,
  "type": "module",
  "workspaces": [
    "services/mcp-gateway"
  ],
  "scripts": {
    "build": "npm --workspace services/mcp-gateway run build",
    "test": "npm --workspace services/mcp-gateway run test",
    "typecheck": "npm --workspace services/mcp-gateway run typecheck",
    "lint": "npm --workspace services/mcp-gateway run lint"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}

