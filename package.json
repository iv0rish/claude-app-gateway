{
  "name": "claude-app-gateway-infra",
  "private": true,
  "type": "module",
  "workspaces": [
    "services/llm-gateway",
    "services/mcp-gateway"
  ],
  "scripts": {
    "build": "npm --workspaces run build",
    "test": "npm --workspaces run test",
    "typecheck": "npm --workspaces run typecheck",
    "lint": "npm --workspaces run lint"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
