apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "claude-apps-gateway.fullname" . }}-config
  namespace: {{ include "claude-apps-gateway.namespace" . }}
  labels:
    {{- include "claude-apps-gateway.labels" . | nindent 4 }}
data:
  gateway.yaml: |
    {{- tpl .Values.gatewayConfig . | nindent 4 }}
